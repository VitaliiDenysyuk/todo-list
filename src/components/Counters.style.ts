import styled from "styled-components";

const CountersStyled = styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  font-size: 1rem;
  font-family: cursive;
  font-style: italic;
  text-transform: uppercase;
  margin: 0;
  padding: 5px 0 5px 0;
`;

export default CountersStyled;
